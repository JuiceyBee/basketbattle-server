{
  "name": "basketbattle-server",
  "version": "2.0.0",
  "description": "BasketBattle sync server — lists, battles, history, specials proxy",
  "main": "server.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  }
}
